# Administrative control boundary

Scripts in this directory manage recovery, supervision, and credentials. They are intentionally outside `bin/` so the MCP generic project-command tool cannot execute them.
